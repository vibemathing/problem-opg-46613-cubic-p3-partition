# ROOT-R07 nonterminal resume checkpoint

`verdict=candidate_only`. This is a Markdown checkpoint, not a WEB_ATTEMPT_PACKET, a research-ledger record, a verifier receipt or an admission.

## Binding

- Repository: `vibemathing/problem-opg-46613-cubic-p3-partition`
- Coordination Issue: #3 only; do not create another research Issue.
- Problem: `problem:opg-46613-cubic-p3-partition`
- ProblemContract SHA-256: `f22ec1937d8b771614cb85435b6d4e0712afb9a3939780e04b74e4649617c26a`
- Harness 1.1.2 declared snapshot SHA-256: `9ff6002285f1cc3ee7bec0a201a09d048dc0fa95a3156120d20aeb9b4177858a`
- Attempt: `attempt:web-20260906-opg46613-a01`
- Route: `route:two-factor-divisible-cycles-v1`
- Graph: `graph:opg46613-initial-v1`
- Current proof target: `obligation:opg46613-root`
- Requested branch: `web/attempt-opg46613-root-r07`
- Primary owner: `math-proof`.

The legacy route identity is retained for transport. Its false divisible-two-factor assertion is not used as a premise, and the canonical DAG is not edited.

## Readback and transport status

The last readable main inherited at the start of this continuation was `85f4b0269addb746adad975cedd1fed07565ee96` (ROOT-R06). This is not a declaration of the current main and must not be copied blindly into a new packet.

Branch creation from main and the proof writes listed below have been requested. Subsequent tool results have not supplied readable operation receipts in the working context. The same limitation affected fresh reads, ordinary local diagnostic output and publication searches. It is not an observed HTTP status, permission revocation, mathematical obstruction or quota-exhaustion signal.

Requests may have taken effect. Inspect the real branch, files and open PRs before retrying. Do not recreate the branch or overwrite an existing candidate blindly. No R07 packet, PR, check result, review, merge, current branch SHA or exact artifact SHA-256 is claimed here. In particular, do not fabricate hashes from previously prepared prose or replace SHA-256 by a Git blob SHA.

The earlier blocked executable upload is not being retried through another endpoint or encoding. This continuation has prepared natural-language proof candidates and a statement-boundary note, not a claimed executable census.

## Requested proof artifacts

All names below are relative to `research/artifacts/candidates/opg46613-root-r07/`:

1. `one-defect-repair.md`
2. `source-aware-transits.md`
3. `cycle-local-minimum-family.md`
4. `complete-one-defect-cycle-interface.md`
5. `neutral-defect-transitions.md`
6. `girth-five-cycle-minima.md`
7. `cyclic-five-cycle-minima.md`
8. `root-equivalences.md`
9. `triangle-free-root-reduction.md`

Source/statement note requested: `research/artifacts/source-notes/opg46613-root-r07-statement-boundaries.md`.

Every file requires exact-byte read-back and SHA-256 before inclusion in a packet. No mathematical claim in these files is promoted by its transport.

## Best candidate and exact frontier

`best_verified_result=none` and `best_verified_candidate=none` under admitted-verifier semantics.

The strongest auxiliary-rule obstruction candidate is `cyclic-five-cycle-minima.md`: for every q>=0, an explicit positive P3 host of order 42+6q, girth six and cyclic edge connectivity exactly five supports a one-defect charged flow with no strictly decreasing simple-cycle update. Only its base graph is asserted Hamiltonian; the subsequent six-vertex expansions explicitly preserve a P3-factor. The all-cycles nondecrease claim is proved by incidence counting, not by an unreported cycle enumeration.

The neutral-repair frontier is `neutral-defect-transitions.md` together with the complete twelve-pattern interface. A one-defect charged flow automatically has a six-vertex exceptional tree and ordinary P3 components. For a cycle update, the exact defect/weight change is r+z-eta. Neutral moves either retain the defect without exceptional source/leaf incidences or remove it and create exactly one elsewhere. A general source-edge potential change contains e(A,C minus R), which cannot be discarded outside the homogeneous case. A repair difference at a strict-cycle minimum must have a cubic bipartite branch core after suppressing degree-two paths; no universal theta or bounded-branch theorem has been proved.

Two root-oriented candidates provide actual global equivalents rather than the failed strengthening:

- `root-equivalences.md`: root is equivalent to deletion of every specified vertex in the order-4-modulo-6 cubic 3-connected domain and to avoidance of every specified edge in the order-0-modulo-6 domain. The explicit reductions have order multipliers and do not turn a pointwise factor into arbitrary edge avoidance.
- `triangle-free-root-reduction.md`: an explicit 45-vertex girth-six three-port cell has exactly the triangle's eight P3 signatures. Replacing all triangles preserves factor existence in both directions and yields a triangle-free root-domain graph of order at most 15n. This is an expansive global reduction, not a proof that a minimum counterexample contains no triangle, and not an unconditional reduction to girth six or cyclic connectivity five.

The last construction uses a fully displayed 18-vertex bipartite Hamiltonian auxiliary graph and three explicit Hamiltonian cycles covering all incident-edge pairs at one vertex. Its intermediate 21-vertex cell has 000, BBB and all six 0AB signatures but lacks AAA. Two copies and three new terminals give the 45-vertex triangle interface. All these are finite proof objects; no unobserved program output is needed for their statements.

## Coverage, failures and certificates

No new exhaustive graph-order census, SAT/UNSAT run or executable certificate was obtained in this continuation. The explicit orders 18, 24+6q, 36+12q and 42+6q refer to particular supplied examples/families, not all cubic graphs at those orders. Preserve earlier census records and retrieve their actual scope rather than overwriting them from memory.

The old divisible-two-factor failure remains preserved. The new failed assertion is unconditional strict simple-cycle descent from a one-defect flow, including its versions restricted to girth-six cyclically-five-connected hosts. All displayed hosts have P3-factors; there is no root counterexample certificate. No canonical failed-route ledger has been edited. A schema-level failed-route proposal needs an actually computed fingerprint when packet transport is reconciled.

## Open obligations and next action

Open canonical obligations remain `obligation:opg46613-root` and `obligation:opg46613-divisible-two-factor`. Source-faithfulness, proof checking and admission remain separate from candidate transport.

First recover the actual remote state: fresh-read main, Issue #3, open PRs, this branch and all-inbox packets; reconcile each pending write; obtain exact file bytes and hashes; then complete exactly one R07 packet and its existing or new PR, with the real base and PR number/URL. Read `web-attempt-packet`, `web-harness-snapshot` and `web-pr-diff-boundary` on the final head, and merge only after all pass and the full diff remains in candidate paths. If already merged, start the next unique packet from the new real main.

The next mathematical work is neutral-plateau control or a constructive use of the genuine root equivalents and triangle-free reduction. A proposed improvement must not assume existence of a one-defect flow for every root graph, must not extrapolate the homogeneous secondary-potential argument, and must not infer failure of P3 decomposition from failure of a restricted repair.

No terminal, background-running or quota-forced status is claimed by this checkpoint. The root remains unresolved in the repository admission sense.
